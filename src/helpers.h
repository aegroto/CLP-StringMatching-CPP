/** utils functions file **/
#ifndef HELPERS_h
#define HELPERS_h

#include <queue>

namespace helpers {
    inline int& max(int& a, int& b);
    int maxPeriod(const char*, int);
    int log2(int);
};

#endif
